{
  "name": "hexmate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "oxlint --deny-warnings",
    "typecheck": "tsc -b",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "check": "npm run check:core && npm run test:pwa",
    "check:core": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build",
    "check:ci": "npm ci --ignore-scripts && npm run check:core && npm run test:pwa:smoke && git diff --exit-code",
    "prepare": "git config --local core.hooksPath .githooks",
    "preview": "vite preview",
    "test:pwa": "node --experimental-strip-types --test tests/*.browser.ts",
    "test:pwa:smoke": "node --experimental-strip-types --test --test-skip-pattern='^(Hits and misses|Local mode|Campaign)' tests/*.browser.ts"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@tanstack/react-router": "^1.170.38",
    "@tanstack/router-plugin": "^1.168.40",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "oxlint": "^1.85.0",
    "playwright-core": "^1.63.0",
    "prettier": "^3.9.8",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
